<!-- 
==========================================
MODIFICATIONS DANS : index.html
==========================================
-->

<!-- --- AVANT (Zone de tes filtres en haut de page) --- -->
<select id="managerSelect" onchange="window.applyFilters()">
    <!-- options -->
</select>
<select id="supervisorSelect" onchange="window.applyFilters()">
    <!-- options -->
</select>

<!-- --- APRÈS --- -->
<!-- Ajout de l'attribut "multiple" pour permettre la multi-sélection -->
<select id="managerSelect" multiple onchange="window.applyFilters()">
    <!-- options -->
</select>
<select id="supervisorSelect" multiple onchange="window.applyFilters()">
    <!-- options -->
</select>

<!-- ========================================== -->

<!-- --- AVANT (Dans la structure de tes cartes, si tu les as en dur ou en template) --- -->
<div class="card-header">
    <h3>Nom du Superviseur</h3>
</div>

<!-- --- APRÈS --- -->
<!-- Ajout du bouton d'impression (Si tu génères ce HTML via JavaScript dans renderCards, il faut ajouter ce bouton dans ta template JS avec ${card.name}) -->
<div class="card-header">
    <h3>Nom du Superviseur</h3>
    <button class="print-single-btn" onclick="window.printSingleTimesheet('Nom du Superviseur')">
        🖨️ Imprimer la fiche
    </button>
</div>
